# Client

Vite + React + Tailwind CSS v4.

```sh
npm install
npm run dev
```

Production build output goes to `dist/`:

```sh
npm run build
```

Preview the production build:

```sh
npm run preview
```
